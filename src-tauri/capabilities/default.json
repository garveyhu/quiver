{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the Quiver main window needs: core event listen + the run_demo_task command.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
